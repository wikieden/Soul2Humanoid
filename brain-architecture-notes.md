# 机器人大脑架构设计笔记

> 从 Figure Helix 02、PI π0.7 的技术路线中提炼的架构设计思考。
> 专注于「机器人大脑」各层之间的通信方式与设计取舍。

---

## 一、Figure Helix 02：三层频率分层架构

Figure AI 在 2026.01 发布的 Helix 02 将架构从两层扩展为三层：

```
频率域分层
──────────────────────────────────────────────────
S2 (语义推理)   7-9 Hz     7B VLM       场景理解、语言、任务分解
      ↓ 输出：continuous latent vector（~4096 维）
S1 (视觉运动)   200 Hz      80M          全传感器→全关节控制
      ↓ 输出：全身关节目标（32 维）
S0 (物理执行)   1 kHz       10M          平衡、接触、协调
      ↓
      执行器扭矩
```

### 各层规格

| 层 | 参数 | 频率 | 输入 | 输出 | 训练数据 |
|---|------|------|------|------|---------|
| **S0** | 10M | 1 kHz | 全身关节状态 + 基座运动指令 | 关节级执行器命令 | 1000+ 小时人类运动数据 |
| **S1** | 80M Transformer | 200 Hz | 头/掌相机、指尖触觉、全身 proprioception | 全身关节目标 | 仿真 20 万并行环境 + 真实 |
| **S2** | 7B 开源 VLM | 7-9 Hz | 单目图像 + 机器人状态 + 语言指令 | 单个 continuous latent vector | 互联网预训练 |

### S0 的关键意义

S0 是一个 10M 参数的运动先验（motion prior），替换了 **109,504 行手写 C++**（MPC 求解器、步态生成器、ZMP 平衡控制器等经典机器人学代码）。这是「学习的 vs 手写的底层控制」的阈值拐点。

---

## 二、两层架构间的通信方式对比

### 核心问题

```
S2（高层语义）
  ↓ 怎么传？
S1（低层执行）
```

有三种主流方案：

### 方案 A：Figure 方式 — latent vector 传递

```
S2 → 单个连续 latent vector（4096 维 float32） → S1
```

| 维度 | 数值 |
|------|------|
| 一个 token 比特数 | 32 比特 |
| 每次传输维度 | 4096 |
| 总信息量 | ~131,072 比特 |

**优点**：
- 信息密度高：可表达精确空间关系、力度、姿态、运动风格
- 端到端可微：S1 的梯度可回流到 S2，S2 学会输出对 S1 最优的表示
- 可承载 7B 模型的完整压缩流形

**缺点**：
- 完全不可调试：S2 输出乱码时人类无法理解
- 无法人工干预：不能直接修改 latent 来纠正行为
- 跨模型迁移困难：换 S1 得重新训练 S2→S1 接口

### 方案 B：纯文本传递

```
S2 → 自然语言文本（64 token, BPE 词表 32K） → S1
```

| 维度 | 数值 |
|------|------|
| 一个 token 比特数 | ~15 比特 |
| 每次传输 token 数 | ~64 |
| 总信息量 | ~960 比特 |

约 latent vector 的 **1/100**。

**优点**：
- 可读可调试：S2 输出了什么改一眼就知道
- 可人工干预：直接改文本「不是那个杯子，是蓝色的」
- 可组合：接任意工具/API/模型，无需重新训练
- 极度鲁棒：加几个字、改同义词，意思不变

**缺点**：
- 信息损失大：

| 信息类型 | latent | 文本 |
|---------|--------|------|
| "拿起杯子"（语义） | ✅ | ✅ |
| "用 3N 的力捏杯把"（力度） | ✅ 连续值 | ❌ 只能写"轻轻"或"用力" |
| "手腕转 23 度"（精确姿态） | ✅ 连续值 | ❌ 说不精确 |
| "杯子在桌子左上角，距右墙 40cm"（空间关系） | ✅ 隐式编码 | ❌ 离散化后丢失 |
| "5 秒前见过的同一个杯子"（跨时间同一性） | ✅ 向量相似度 | ❌ 文本无法保证指代同一性 |
| "看起来像这样做但快一点"（风格/韵律） | ✅ | ❌ |
| S2 全部 7B 模型知识 | ✅ 4096 维压缩流形 | ❌ 64 token ≈ 0.0000001% |

- **端到端不可微**：文本离散不可微，S2 无法从下游执行误差中学习
- **对齐损失**：S2 学到的不是「对执行最优的表达」，而是「人类能理解的表达」

### 方案 C：π0.7 方式 — 文本 + 副目标图像（混合）

```
高层策略（同架构）→ 自然语言子任务 + 副目标图 + 元数据 → π0.7 本体
                     ↑ 文本                ↑ 视觉连续           ↑ 结构化标签
```

这是折中方案。**文本承载语义信号，副目标图像承载精确空间信号**。文本损失的精确空间信息，被副目标图补回来了。

| 传递内容 | 形式 | 携带什么信息 |
|---------|------|------------|
| "把袖子折到中间" | ✅ 文本 | 语义意图 |
| 副目标图（左边已折好的画面） | ✅ 视觉（连续） | 精确空间信息 |
| 质量/速度标签 | ✅ 结构化文本 | 执行风格 |

---

## 三、架构类型总览

| 架构 | 代表 | 总参数 | 通信方式 | 分层逻辑 |
|------|------|--------|---------|---------|
| **三层频率分层** | Figure Helix 02 | ~7.1B | S2→latent→S1→joint targets→S0 | 频率（7Hz→200Hz→1kHz） |
| **单模型 + 丰富信号** | PI π0.7 | ~5B | 文本+副目标图+元数据→统一模型 | 功能（同架构不同 prompt） |
| **双系统** | Figure Helix 第一代 | ~7.1B | S2→latent→S1 | 快慢（7Hz→200Hz） |
| **传统分层** | Boston Dynamics | 小 | 状态→MPC→控制器 | 算法类型（规划→控制→执行） |

### 哲学差异

```
Figure:
  "我按频段拆——慢的想策略，快的做控制，最快的做物理。
   每层都是极致优化的专用网络，层间用连续向量通信。"

PI π0.7:
  "我不显式分层。我给一个模型足够丰富的信号（副目标、元数据、子步骤），
   它自己学会在不同粒度上推理。通信不是传向量，是传文本+图像。"

你（L3 大脑 + 文本接口）:
  "我用 LLM 做大脑，输出文本给下层。
   文本损失的视觉空间信息，靠下层自己能看场景来补。"
```

---

## 四、实践建议

### 接口选择决策树

```
S1（下层）有独立视觉输入？
├── 是 → 文本传递够用
│   └── S1 自己能看到场景，文本只需告诉它「关注哪里、要做什么」
│
└── 否 → 文本是严重瓶颈
    ├── 方案 A：用 latent vector（信息全但不可调试）
    └── 方案 B：用文本 + 副目标图像（π0.7 方式，折中）
```

### 按场景推荐

| 场景 | 推荐通信方式 | 理由 |
|------|------------|------|
| 任务分解（规划层→执行层） | **文本** ✅ | 人类可调试，灵活，够用 |
| 精细操作（姿势/力控） | **latent** 或 **图像** | 文本损失的连续信息致命 |
| 长程任务（>1 分钟） | **文本 + 图像** | 文本做粗粒度规划，图像补精确空间 |
| 快速原型验证 | **文本** ✅ | 可读可调，跑通再优化 |

### 一句话结论

> 如果下层有独立的视觉输入，S2→S1 用文本传递**损失可控**——下层自己能看到场景，文本只是注意力引导。如果下层没有视觉、全靠上层喂信息，文本接口就是严重瓶颈，必须用 latent 或副目标图像来补足空间信息。

---

## 五、更新记录

| 日期 | 内容 |
|------|------|
| 2026-06-16 | 初稿，对比 Figure Helix 02、PI π0.7、文本接口三种架构的层间通信设计 |
